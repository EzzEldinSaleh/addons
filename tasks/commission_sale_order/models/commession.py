from odoo import api, fields, modelsimport datetimeclass commisstionsale(models.Model):    _name = 'commission'    amount_from = fields.Float("Amount From")    amount_to = fields.Float("Amount to")    amount_des = fields.Float("deserved amount")    @api.constrains()    def open_wizard(self):        return {            'type': 'ir.actions.act_window',            'res_model': "wizard.comm",            'view_mode': "form",            'target': 'new',        }class saleorders(models.Model):    _inherit = 'sale.order'    is_check = fields.Boolean(string="is",  )